name: Continuous integration
on: [push]

jobs:
  build-emscripten:
    runs-on: ubuntu-latest
    steps:
      - uses: ecco/setup-emscripten@master
      - uses: actions/checkout@v1
      - run: source emsdk_env.sh && make PLATFORM=emscripten
