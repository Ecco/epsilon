name: Continuous integration
on: [push]

jobs:
  build-emscripten:
    runs-on: windows-latest
    steps:
      - uses: ecco/setup-msys2@master
      - uses: actions/checkout@v1
      - run: make PLATFORM=emscripten
